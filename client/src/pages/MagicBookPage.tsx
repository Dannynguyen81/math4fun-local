/**
 * Field Journal Quest — Magic Book page.
 * Design reminder: a tactile parchment archive where each ấn feels like evidence from an expedition.
 */
import { useEffect, useMemo, useState } from "react";
import { Link } from "wouter";
import { Award, BookMarked, Lock, Play, ScrollText, ShieldCheck, Sparkles, Volume2 } from "lucide-react";
import { ELEMENTAL_MATCHUPS, ELEMENT_ORDER, ELEMENT_XP_PER_LEVEL, GUARDIANS, MAGIC_MEDIA, type ElementName } from "@/game/gameData";
import { useGame } from "@/contexts/GameContext";
import { playElementSound } from "@/lib/magicAudio";

const ELEMENT_COLORS: Record<ElementName, string> = { "sấm": "#f6b73c", "lửa": "#ee6b4e", "nước": "#55a9dd", "độc": "#8e69ad", "gió": "#3e9b7a", "đất": "#b17a3d" };
const ELEMENT_SIGILS: Record<ElementName, string> = { "sấm": "◇", "lửa": "✦", "nước": "≈", "độc": "✥", "gió": "❋", "đất": "◆" };

export default function MagicBookPage() {
  const { profile, audioEnabled, markMagicVideoWatched, magicBookWatchedCount, hasMagicBookAchievement, elementXp, elementLevel, weeklyMagicQuest } = useGame();
  const unlockedByElement = useMemo(() => {
    const elementMap = new Map<ElementName, string>();
    for (const guardianId of profile?.collectedGuardianIds ?? []) {
      const guardian = GUARDIANS.find((item) => item.id === guardianId);
      if (guardian && !elementMap.has(guardian.element)) elementMap.set(guardian.element, guardian.id);
    }
    return elementMap;
  }, [profile?.collectedGuardianIds]);
  const [selectedElement, setSelectedElement] = useState<ElementName>("lửa");
  const selectedUnlocked = unlockedByElement.has(selectedElement);
  const selectedMedia = MAGIC_MEDIA[selectedElement];
  const selectedGuardian = GUARDIANS.find((item) => item.id === unlockedByElement.get(selectedElement));
  const selectedXp = elementXp[selectedElement] ?? 0;
  const selectedProgress = selectedXp % ELEMENT_XP_PER_LEVEL;

  useEffect(() => {
    const firstUnlocked = ELEMENT_ORDER.find((element) => unlockedByElement.has(element));
    if (firstUnlocked && !unlockedByElement.has(selectedElement)) setSelectedElement(firstUnlocked);
  }, [selectedElement, unlockedByElement]);

  return <section className="magic-book-page"><header className="magic-book-cover"><div><p className="section-kicker"><BookMarked size={14} /> LOCAL MAGIC ARCHIVE</p><h1 className="font-display text-4xl font-black tracking-tight">Sổ Phép Ngũ Hành</h1><p>Ghi lại hoạt ảnh và âm thanh của các hệ em đã thật sự thu phục. Mỗi dấu niêm phong cần một guardian đồng hành để mở.</p></div><div className="magic-book-count"><span>{unlockedByElement.size}</span><small>/ 5 hệ đã mở</small></div></header><div className="magic-book-route"><span className="field-tag">TUYẾN TƯ LIỆU</span><i /><b>{unlockedByElement.size}/5 dấu đã định danh</b><i /><small>Mốc tiếp theo: {unlockedByElement.size === 5 ? "Nhà Lưu Trữ Ngũ Ấn" : "thu phục guardian mới"}</small></div><section className="mt-5 grid gap-4 md:grid-cols-[minmax(0,1fr)_280px]"><article className="border-2 border-[#172a48] bg-[#fffdf6] p-4 shadow-[4px_4px_0_#172a48]"><div className="flex items-start gap-3"><span className="mt-0.5 text-2xl" style={{ color: ELEMENT_COLORS[selectedElement] }}>{ELEMENT_SIGILS[selectedElement]}</span><div className="min-w-0"><p className="section-kicker">NHẬT KÝ ẤN ĐANG XEM</p><h2 className="font-display text-2xl font-black">{selectedMedia.shortLabel} · Bậc {elementLevel(selectedElement)}</h2><div className="mt-2 h-3 overflow-hidden border border-[#172a48] bg-[#f5efdf]"><div className="h-full" style={{ width: `${(selectedProgress / ELEMENT_XP_PER_LEVEL) * 100}%`, backgroundColor: ELEMENT_COLORS[selectedElement] }} /></div><p className="mt-1 text-xs font-bold text-[#476275]">{selectedXp} XP · {selectedProgress}/{ELEMENT_XP_PER_LEVEL} đến bậc luyện ấn tiếp theo.</p></div></div></article><aside className="border-2 border-[#172a48] bg-[#172a48] p-4 text-white shadow-[4px_4px_0_#f6b73c]"><div className="flex items-center gap-2 text-[#f6b73c]"><ScrollText size={17} /><p className="text-xs font-bold tracking-[0.15em]">NHIỆM VỤ TUẦN</p></div>{weeklyMagicQuest && <><p className="mt-2 font-display text-xl font-black">{weeklyMagicQuest.title}</p><p className="mt-1 text-xs leading-relaxed text-[#d5dfed]">Dùng {MAGIC_MEDIA[weeklyMagicQuest.element].shortLabel} {weeklyMagicQuest.target} lần: {weeklyMagicQuest.usedCount}/{weeklyMagicQuest.target}.</p><p className="mt-2 text-xs font-bold text-[#f6b73c]">{weeklyMagicQuest.rewardClaimed ? "Phần thưởng đã ghi vào hồ sơ" : `Phần thưởng +${weeklyMagicQuest.rewardXp} XP`}</p></>}</aside></section><div className="mt-6 grid gap-5 xl:grid-cols-[minmax(0,1fr)_350px]"><div className="magic-sigil-grid" aria-label="Các dấu ấn Ngũ hành">{ELEMENT_ORDER.map((element) => { const guardian = GUARDIANS.find((item) => item.id === unlockedByElement.get(element)); const unlocked = Boolean(guardian); const selected = element === selectedElement; return <button key={element} disabled={!unlocked} onClick={() => setSelectedElement(element)} className={`magic-sigil-card ${selected ? "is-selected" : ""} ${unlocked ? "is-unlocked" : "is-locked"}`} style={{ "--magic-color": ELEMENT_COLORS[element] } as React.CSSProperties}><span className="magic-sigil-mark">{ELEMENT_SIGILS[element]}</span><span className="magic-sigil-copy"><b>{MAGIC_MEDIA[element].shortLabel}</b><small>{unlocked ? guardian?.name : "Cần thu phục guardian"}</small></span>{unlocked ? <Sparkles size={16} /> : <Lock size={15} />}</button>; })}</div><aside className="magic-book-viewer" style={{ "--magic-color": ELEMENT_COLORS[selectedElement] } as React.CSSProperties}>{selectedUnlocked && selectedGuardian ? <><div className="magic-viewer-heading"><span className="field-tag"><Sparkles size={13} /> ĐÃ MỞ</span><h2>{selectedMedia.title}</h2><p>{selectedMedia.note} Guardian giữ dấu này: <b>{selectedGuardian.name}</b>.</p></div><video key={selectedElement} src={selectedMedia.src} controls playsInline preload="metadata" onEnded={() => markMagicVideoWatched(selectedElement)} aria-label={`Video ${selectedMedia.title}`} /><button onClick={() => playElementSound(selectedElement, audioEnabled, "cast")} className="magic-sound-button"><Volume2 size={17} /> Nghe lại âm phép</button></> : <div className="magic-locked-viewer"><Lock size={28} /><h2>Trang này còn niêm phong</h2><p>Thu phục một guardian thuộc hệ {MAGIC_MEDIA[selectedElement].shortLabel.toLowerCase()} để xem lại clip và nghe âm phép.</p>{profile ? <Link href="/map" className="magic-sound-button"><Play size={17} /> Đi đến Bản đồ học</Link> : <Link href="/start" className="magic-sound-button"><Play size={17} /> Tạo hồ sơ thám hiểm</Link>}</div>}</aside></div><section className={`magic-achievement ${hasMagicBookAchievement ? "is-earned" : ""}`}><Award size={28} /><div><p className="section-kicker">HUY HIỆU HỒ SƠ</p><h2>{hasMagicBookAchievement ? "Nhà Lưu Trữ Ngũ Ấn" : "Ngũ Ấn Chờ Mở"}</h2><p>{hasMagicBookAchievement ? "Em đã xem trọn năm tư liệu phép; huy hiệu này là bằng chứng của một nhà thám hiểm biết quan sát." : `Đã xem ${magicBookWatchedCount}/5 hoạt ảnh đến hết. Hoàn thành Ngũ Ấn để nhận huy hiệu đặc biệt.`}</p></div><span>{hasMagicBookAchievement ? <ShieldCheck size={28} /> : `${magicBookWatchedCount}/5`}</span></section><section className="magic-matchup-sheet"><div className="magic-matchup-heading"><div><p className="section-kicker">BẢNG TƯƠNG KHẮC · NGŨ HÀNH MATH4FUN</p><h2>Luật đọc dấu Ngũ hành</h2></div><p>Đây là quy ước chiến thuật trong game; đáp án Toán luôn là nền tảng để phép được kích hoạt.</p></div><div className="magic-matchup-list">{ELEMENT_ORDER.map((element) => { const matchup = ELEMENTAL_MATCHUPS[element]; return <article key={element} style={{ "--magic-color": ELEMENT_COLORS[element] } as React.CSSProperties}><span className="magic-sigil-mark">{ELEMENT_SIGILS[element]}</span><div><b>{MAGIC_MEDIA[element].shortLabel}</b><p><em>Khắc:</em> {MAGIC_MEDIA[matchup.strongAgainst].shortLabel} · <em>Bị khắc:</em> {MAGIC_MEDIA[matchup.weakAgainst].shortLabel}</p><small>{matchup.fieldNote}</small></div></article>; })}</div></section></section>;
}